diff --git a/net/bluetooth/hci_sync.c b/net/bluetooth/hci_sync.c
index f79b38603205..f9fa2086e8fb 100644
--- a/net/bluetooth/hci_sync.c
+++ b/net/bluetooth/hci_sync.c
@@ -2990,12 +2990,13 @@ static int hci_le_set_ext_scan_param_sync(struct hci_dev *hdev, u8 type,
 		phy++;
 	}
 
+	/*
 	if (scan_coded(hdev)) {
 		cp->scanning_phys |= LE_SCAN_PHY_CODED;
 		hci_le_scan_phy_params(phy, type, interval * 3, window * 3);
 		num_phy++;
 		phy++;
-	}
+	}*/
 
 done:
 	if (!num_phy)
@@ -6469,7 +6470,7 @@ static int hci_le_ext_create_conn_sync(struct hci_dev *hdev,
 		p++;
 		plen += sizeof(*p);
 	}
-
+/*
 	if (scan_2m(hdev) && (conn->le_adv_phy == HCI_ADV_PHY_2M ||
 			      conn->le_adv_sec_phy == HCI_ADV_PHY_2M)) {
 		cp->phys |= LE_SCAN_PHY_2M;
@@ -6485,7 +6486,7 @@ static int hci_le_ext_create_conn_sync(struct hci_dev *hdev,
 		set_ext_conn_params(conn, p);
 
 		plen += sizeof(*p);
-	}
+	}*/
 
 	return __hci_cmd_sync_status_sk(hdev, HCI_OP_LE_EXT_CREATE_CONN,
 					plen, data,
